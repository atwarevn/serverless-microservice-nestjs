import { MapQueryServiceImpl } from './map-query.service-impl';

describe('MapQueryServiceImpl', () => {
  it('should be defined', () => {
    expect(new MapQueryServiceImpl()).toBeDefined();
  });
});
